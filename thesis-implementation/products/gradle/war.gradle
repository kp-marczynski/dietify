apply plugin: "war"

bootWar {
    mainClassName = "pl.marczynski.dietify.products.ProductsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
