apply plugin: "war"

bootWar {
    mainClassName = "pl.marczynski.dietify.appointments.AppointmentsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
